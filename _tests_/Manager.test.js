const manager = require('../lib/manager');

describe('Manager', () => {
    describe('Initialize', () => {
        // tests for constructor, return string (or number), etc
    })

    describe('getRole', () => {
        
    })
}